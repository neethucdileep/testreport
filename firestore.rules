rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Emulator/dev rules only. Tighten for production before deploy.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}

